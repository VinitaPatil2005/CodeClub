const Achievements = () => {
    return (
      <section id="achievements" className="section achievements">
        <h2>Our Achievements</h2>
        <p>We are proud of the milestones we have achieved together. From successful events to innovative projects, our members continuously push the boundaries.</p>
        <button onClick={() => alert('Discover our achievements!')}>See Achievements</button>
      </section>
    );
  };
  
  export default Achievements;
  