const Events = () => {
  return (
    <section id="events" className="section events">
      <h2>Our Events</h2>
      <p>Join our tech workshops, hackathons, and meetups to enhance your skills and network with industry leaders!</p>
      <button onClick={() => alert('Check out our events schedule!')}>View Events</button>
    </section>
  );
};

export default Events;
